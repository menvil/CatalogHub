# Central Admin presentation context

Namespace: `App\Filament\Central`.

Owns the Central Admin shell, pages and resources introduced for the Central context. It may use shared application, domain and UI primitives, but must not import Site Admin pages or resources. Legacy classes under `App\Filament` remain in place until separately scoped migration tasks authorize moving them.

# Site Admin presentation context

Namespace: `App\Filament\Site`.

Owns the Site Admin shell, pages and resources introduced for the Site context. It may use shared application, domain and UI primitives, but must not import Central Admin pages or resources. Phase 0.2 does not resolve a site from a domain or route parameter.

@props(['config' => []])

<section data-theme-block="latest_reviews">
    <h2>{{ $config['title'] ?? 'Latest reviews' }}</h2>
    <p>Review data will be available when the reviews module is introduced.</p>
</section>

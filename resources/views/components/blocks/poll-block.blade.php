@props(['config' => []])

<section data-theme-block="poll_block">
    <h2>{{ $config['title'] ?? 'Community poll' }}</h2>
    <p>Poll voting will be available when the polls module is introduced.</p>
</section>

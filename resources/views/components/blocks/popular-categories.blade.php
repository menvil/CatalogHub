@props(['config' => []])

<section data-theme-block="popular_categories">
    <h2>{{ $config['title'] ?? 'Popular categories' }}</h2>
    <p>Category projection data will be available in a later phase.</p>
</section>

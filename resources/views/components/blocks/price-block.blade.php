@props(['config' => []])

<section data-theme-block="price_block">
    <h2>{{ $config['title'] ?? 'Prices and offers' }}</h2>
    <p>Price source and offer data will be available in a later phase.</p>
</section>

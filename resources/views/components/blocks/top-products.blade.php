@props(['config' => []])

<section data-theme-block="top_products">
    <h2>{{ $config['title'] ?? 'Top products' }}</h2>
    <p>Product projection data will be available in a later phase.</p>
</section>
